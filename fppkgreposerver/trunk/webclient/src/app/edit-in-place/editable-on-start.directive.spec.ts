import { EditableOnStartDirective } from './editable-on-start.directive';

describe('EditableOnStartDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableOnStartDirective();
    expect(directive).toBeTruthy();
  });
});
